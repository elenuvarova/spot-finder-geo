# Base URL of the SpotFinder backend API.
# In local dev this is the FastAPI server; in production point it at the deployed
# (e.g. Render) backend. NEVER put any API key here — keys live on the backend only.
VITE_API_URL=http://localhost:8000
